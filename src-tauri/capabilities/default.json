{
  "identifier": "default",
  "description": "Default capabilities for the app",
  "windows": ["main", "display"],
  "permissions": [
    "core:default",
    "shell:allow-open",
    "dialog:allow-open"
  ]
}